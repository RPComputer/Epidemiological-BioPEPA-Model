We didn't really write the .cshrc

When you work in the cs cluster of UCSB
Please use gcc296 instead of the gcc
and use the mpiCC in the /usr/local/bin(also based on gcc296)
instead of the /usr/bin/(based on gcc 3.3.2)
